# Adapters

This package owns external IO boundaries.

Put code here when it:

- calls an external API,
- speaks a provider protocol,
- refreshes credentials,
- normalizes external errors or payloads.

Adapters do not own business decisions.
