# Application Layer

This package owns use-case orchestration.

Put code here when it:

- coordinates multiple domain modules,
- invokes one or more engines,
- defines transaction boundaries,
- exposes a business workflow to API/web transport.

Do not put raw provider code or generic utilities here.
