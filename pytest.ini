[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    ignore:Inheritance class AiohttpClientSession from ClientSession is discouraged:DeprecationWarning:google\.genai\._api_client
markers =
    flaky: non-deterministic tests quarantined from blocking gates; exercised in dedicated rerun jobs
